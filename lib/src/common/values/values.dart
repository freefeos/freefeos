part of '../common.dart';

final class V {
  const V._();

  /// 通道
  static const VChannel channels = VChannel();

  /// 颜色
  static const VColors colors = VColors();

  /// banner
  static const VDrawable drawable = VDrawable();

  /// 方法
  static const VMethod methods = VMethod();

  /// 占位符
  static const VPlaceholder placeholder = VPlaceholder();

  /// 字符串
  static const VString strings = VString();
}
